import { Grid } from '@material-ui/core';

import styled from '@emotion/styled';

const UsageWrapper = styled(Grid)`
  width: 100%;
  padding-left: 5px;
  margin: auto;
`;

export const Styled = {
  UsageWrapper,
};
