export type TeamMemberShareProps = {
  folderName: string;
};

export type ValuesType = {
  member: string;
  sharePermissions: string;
};

export type MembersStateTypes = { value: string; type: string; groupId: string };
